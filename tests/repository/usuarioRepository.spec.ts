import UsuarioRepository from '@/repository/usuarioRepository'

describe('UsuarioRepository', () => {
  it('Testando chamada UsuarioRepository', () => {
    expect(UsuarioRepository).toBeDefined()
  })
})
